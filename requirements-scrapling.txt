requests
playwright
openpyxl>=3.1
# Scrapling — Camoufox-based stealth fetcher. Wraps a custom Firefox build
# with anti-fingerprinting patches; bypasses many WAFs (Tencent EdgeOne /
# Cloudflare bot mode) without needing a paid solver service. Used by
# autocango scrapers via stealth.fetch_protected_html.
scrapling>=0.2.99
